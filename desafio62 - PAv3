# Melhore o desafio anterior, perguntando para o usuário se ele quer mostrar mais alguns termos. O programa encerra
# quando ele disser que quer mostrar 0 termos.

n = int(input('Primeiro Termo:').strip())
r = int(input('Razão:').strip())
termo = n
cont = 1
mais = 10
total = 0
while mais != 0:
    total += mais
    while cont <= total:
        print(f'{termo}, ', end='')
        termo += r
        cont += 1
    mais = int(input('Quantos termos mais voce quer ver?'))
print('fim')
