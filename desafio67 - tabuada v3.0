# Faça um programa que mostre a tabuada de vários números, um de cada vez, para cada valor digitado pelo usuário. O
# programa será interrompido quando o número solicitado for negativo.
from time import sleep

while True:
    n = int(input('Digite um número para ver sua tabuada [número negativo para parar]: ').strip())
    if n < 0:
        break
    print('=-'*20)
    for cont in range(0, 10):
        print(f'{n} x {cont} = {n * cont}')
    print('=-' * 20)
print('\nENCERRANDO O PROGRAMA', end='')

# loop pra fazer graça
c = 0
while c < 3:
    c += 1
    sleep(0.5)
    print('.', end='')
    
