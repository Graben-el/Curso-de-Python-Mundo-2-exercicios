# Desenvolva um programa que leia 6 números inteiros e mostre a soma apenas daqueles que forem pares,
# se for ímpar desconsidere-o

s = 0
ct = 0
for c in range(1, 7):
    n = int(input(f'Digite o {c}° número:').strip())
    if n % 2 == 0:
        s += n
        ct += 1
print(f'A somatória dos {ct} números pares inseridos é {s}')

